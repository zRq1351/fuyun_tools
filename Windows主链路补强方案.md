# fuyun_tools Windows主链路补强方案

## 1. 文档目的

本文件用于定义 `P0-01 Windows 主链路补强` 的具体方案，目标是在不扩散新业务边界的前提下，优先解决现有高频链路的稳定性、一致性和可预期性问题。

本方案只关注当前 `Windows` 版本，不讨论跨平台抽象。

---

## 2. 问题定义

当前项目虽然功能链路已经较完整，但主链路仍存在以下典型问题：

- 功能可用，但在边界场景下不稳定
- 前后端状态机分散，容易出现表现不一致
- 错误提示、回退逻辑、收尾策略不统一
- 不同窗口之间的显示、焦点、生命周期规则不够收敛
- 异常场景下容易出现“没反应”“状态卡住”“用户不知道下一步怎么做”

`P0` 阶段的目标不是增加新功能，而是让现有功能“更稳、更顺、更可预期”。

---

## 3. 目标

本阶段目标分为五项：

1. 提升选词获取与回写成功率
2. 收敛窗口显示、焦点和生命周期一致性
3. 收敛截图启动、编辑、导出链路
4. 稳定录屏状态机与异常收尾
5. 提升图片历史加载与预览流畅度

## 3.1 当前推进状态（2026-04-12）

- 本轮实现重点落在：
    - `P0-02 长截图默认可用`
    - `P0-03 数据备份与恢复`
    - `P0-04 诊断中心第一版`
- 本轮已开始推进 `P0-01-01 选词获取与回写链路梳理`：
    - 统一回写结果事件 `writeback-result`
    - 回写结果开始显式输出目标前台窗口标题、PID、阶段和失败原因
    - 粘贴前台检查从“仅标题稳定且不是本应用”收敛为“PID + 标题稳定且不是本应用”
    - 结果窗已接入回写成功/失败提示，避免静默失败
    - 文字历史页和图片历史页已接入统一回写结果提示
    - `AppState` 已开始拆分为“划词捕获中 / 文本回写中 / 图片回写中”三类状态，并继续向旧标志位回灌
    - 已新增统一 `writeback-phase` 事件，开始输出等待窗口隐藏、写入剪贴板、执行粘贴、完成/失败等阶段
    - 已新增“原目标窗口快照”记录：划词工具栏、文字历史页、图片历史页、结果窗在抢焦点前都会记录当前外部前台窗口
    - 回写时已优先等待原目标窗口恢复，而不再只要“任意外部窗口稳定”就执行粘贴
    - 有原目标窗口快照时，粘贴前会先尝试主动恢复目标窗口前台，再退回等待逻辑
    - 诊断中心已新增“回写链路状态”卡片，可看到最近一次回写来源、阶段、目标窗口与失败细节
    - 窗口管理层已开始统一 `show / focus / hide` 辅助逻辑，并维护当前活动覆盖层窗口
    - 诊断中心已新增“覆盖层窗口状态”卡片，用于观察工具栏、结果窗、预览窗、剪贴板窗的生命周期一致性
    - `commands.rs` 中结果窗、截图窗、长截图工具条/边框窗等分散入口已开始切到统一覆盖层 helper，减少直接
      `show / hide / set_focus` 的分散调用
    - 静态覆盖层窗口与动态结果窗/长截图窗口已开始统一绑定关闭/销毁事件，窗口主动关闭时也会清理活动覆盖层状态
    - 已记录最近一次覆盖层生命周期动作，诊断页可看到最近一次 `shown / hidden / focused / destroyed`
    - 截图窗与固定图片窗也已接入统一覆盖层生命周期：显示/聚焦/隐藏路径开始复用统一 helper，并绑定关闭事件
    - 诊断页已改为监听覆盖层生命周期与回写结果事件，相关卡片在链路变化时可自动刷新
    - OCR 结果窗也已开始纳入统一覆盖层生命周期管理
    - 覆盖层状态已开始保留最近多条生命周期历史，诊断可看到窗口动作序列而不只是一条最近记录
    - 设置窗已统一到普通窗口 `show / focus + close->hide` helper，录屏工具栏也已切入统一覆盖层显示/关闭语义
- 当前判断：`P0-01-02 窗口显示与焦点恢复一致性治理` 的基础统一层已基本完成，后续主要剩边角窗口和更细业务残留优化
- 与本方案直接相关且已顺带推进的内容：
    - 依赖检查状态已从开发者面板产品化到“诊断与修复”
    - 长截图运行时可用性与截图入口禁用逻辑已补上
- 本方案中的以下主链路治理项仍未在本轮展开：
    - 窗口显示、焦点与生命周期统一
    - 截图导出状态机进一步收口
    - 录屏状态机异常收尾治理
    - 图片历史大数据量性能补强

---

## 4. 设计原则

- 优先根因修复，不做临时绕补
- 优先统一状态机，不继续散落条件分支
- 优先统一错误码、错误提示和修复动作
- 优先确保“失败可见、状态可恢复、结果可验证”
- 优先治理跨窗口一致性，而不是单点局部优化

---

## 5. 治理范围

本方案聚焦以下模块：

- `src-tauri/src/ui/window_manager.rs`
- `src-tauri/src/ui/commands.rs`
- `src-tauri/src/lib.rs`
- `src-tauri/src/features/text_selection.rs`
- `src-tauri/src/features/recording/recorder_service.rs`
- `src-tauri/src/ui/commands_recording.rs`
- `src-tauri/src/features/screenshot/*`
- `src/pages/screenshot/App.vue`
- `src/pages/recording_toolbar/App.vue`
- `src/pages/image_clipboard/App.vue`
- `src/pages/shared/*`

---

## 6. 主链路拆分

## 6.1 选词获取与回写链路

### 当前风险

- 选词成功后回写失败，用户无感知
- 回写去重和实际粘贴行为可能不一致
- 原剪贴板恢复和新内容写入之间存在竞态
- 不同目标应用对复制、粘贴、焦点恢复的容忍度不同

### 方案目标

- 统一选词 -> 复制 -> 判断内容 -> 弹工具栏 -> 回写 -> 恢复原剪贴板的状态流
- 把“是否成功回写”从推测改成显式结果
- 收敛回写失败后的用户提示和回退动作

### 方案要点

#### A. 显式状态阶段

建议统一拆为以下阶段：

- `idle`
- `capturing_selection`
- `selection_ready`
- `writing_back`
- `restoring_clipboard`
- `done`
- `failed`

#### B. 统一超时与失败分支

对以下阶段统一超时策略：

- 选词复制超时
- 目标应用未获得焦点
- 粘贴动作触发失败
- 剪贴板恢复失败

#### C. 统一结果语义

对前端暴露明确结果：

- 回写成功
- 目标应用拒绝粘贴
- 焦点恢复失败
- 原剪贴板恢复失败但主操作成功

### 验收标准

- 常见文本应用中回写成功率稳定提升
- 回写失败不再静默
- 原剪贴板恢复语义一致

---

## 6.2 窗口显示、焦点与生命周期链路

### 当前风险

- 窗口显示逻辑散落在命令层、窗口管理层和页面层
- 打开窗口后焦点不一定恢复到预期位置
- 关闭窗口后残留运行态、选中态或临时状态
- 多窗口之间对显示、隐藏、销毁、复用的语义不统一

### 方案目标

- 统一窗口行为语义
- 收敛“显示 / 隐藏 / 关闭 / 保活 / 复用”的边界
- 明确哪些窗口是临时窗口，哪些是常驻窗口

### 方案要点

#### A. 定义窗口类型

建议至少划分：

- 常驻窗口：设置、主列表、图片列表、录屏胶囊
- 临时结果窗口：OCR、翻译结果、解释结果
- 交互态窗口：截图、长截图边框、长截图工具栏

#### B. 统一生命周期动作

建议统一抽象以下行为：

- `show`
- `hide`
- `focus`
- `reset_runtime_state`
- `close_or_keep_alive`

#### C. 焦点恢复策略统一

对以下场景统一处理：

- 打开截图前的前台窗口记录
- 关闭工具栏后焦点回归
- 回写前确保目标窗口仍为有效目标

### 验收标准

- 高频窗口行为可预期
- 焦点恢复规则一致
- 窗口关闭后无明显状态残留

---

## 6.3 截图启动、编辑与导出链路

### 当前风险

- 截图启动、编辑、保存、复制、钉图涉及前后端多个分支
- 长截图和普通截图在导出链路上存在特殊处理
- 前端认为成功但后端实际未完成的风险必须持续压制

### 方案目标

- 收敛为统一截图任务状态机
- 统一“源图 -> 编辑态 -> 输出结果”的语义
- 所有导出行为都归一到明确后端结果

### 方案要点

#### A. 截图任务状态统一

建议拆为：

- `booting`
- `capturing`
- `editing`
- `exporting`
- `copying`
- `pinning`
- `done`
- `failed`

#### B. 输出动作归一

统一三种输出动作：

- 保存
- 复制
- 钉图

每种动作都要求：

- 有明确开始
- 有明确成功返回
- 有明确失败提示

#### C. 普通截图与长截图共享导出契约

虽然采样源不同，但对导出层统一要求：

- 输入统一为源图路径 + 编辑指令
- 输出统一为后端明确成功结果
- 前端只负责交互，不再自行兜底拼接最终语义

### 验收标准

- 保存、复制、钉图行为稳定
- 导出失败提示明确
- 普通截图与长截图在输出语义上统一

---

## 6.4 录屏状态机与异常收尾

### 当前风险

- 开始、暂停、恢复、停止之间的状态切换容易因高频操作出错
- 录屏依赖缺失、设备切换、异常退出时的收尾路径复杂
- 前端胶囊展示状态和后端实际录制状态可能短暂不一致

### 方案目标

- 收敛录屏状态机
- 明确异常退出的统一收尾逻辑
- 保证用户看到的状态与后端状态尽可能一致

### 方案要点

#### A. 统一状态机

建议状态收敛为：

- `disabled`
- `idle`
- `preparing`
- `recording`
- `paused`
- `stopping`
- `failed`

#### B. 收尾逻辑单入口

无论以下哪种结束路径，都必须走统一收尾：

- 手动停止
- 录制进程退出
- `ffmpeg` 失败
- 空文件失败
- 音视频合成失败

#### C. 胶囊状态刷新单向化

建议以后端真实状态为准，前端胶囊只订阅和展示，不自行推演最终状态。

### 验收标准

- 状态流转不重复、不穿透
- 异常后能恢复到可继续使用状态
- 胶囊展示与后端状态一致

---

## 6.5 图片历史加载与预览流畅度

### 当前风险

- 图片历史属于高频大数据量页面
- 首屏加载、滚动预热、大图打开容易出现性能抖动
- 预热、同步、预览生成可能出现局部重复触发

### 方案目标

- 控制首屏成本
- 保证滚动与大图打开稳定
- 避免重复预热、重复同步、重复事件

### 方案要点

#### A. 继续坚持批量化与 single-flight

- 预热保持批量
- 同一批次资源请求避免重复发起
- 页面尾部与大图打开链路优先用后端批量能力

#### B. 限制状态机重复构建

- 合并逻辑统一走一套 helper
- 热点路径避免重复扫描和重复派生数据

#### C. 区分“必须立即加载”和“可延后加载”

- 首屏卡片元数据优先
- 大图与非可视区资源延后
- 预览缓存缺失时提供明确占位

### 验收标准

- 图片历史滚动无明显卡顿
- 大图预览成功率稳定
- 无重复同步风暴

---

## 7. 通用治理项

## 7.1 错误码与错误文案统一

建议按以下层级统一错误输出：

- 系统错误
- 依赖缺失
- 用户取消
- 目标应用不可达
- 数据写入失败
- 状态冲突

要求：

- 前端展示文案可读
- 后端保留具体诊断信息
- 同类错误统一映射

## 7.2 状态机与事件命名统一

需要统一：

- 状态名
- 事件名
- 前后端结果字段
- 成功 / 失败 / 取消的区分

## 7.3 收尾语义统一

凡是涉及临时资源、临时窗口、临时状态的操作，都必须定义：

- 谁负责清理
- 在什么时机清理
- 清理失败是否影响主结果

---

## 8. 执行顺序建议

建议按以下顺序推进：

1. 选词回写链路
2. 窗口与焦点治理
3. 截图导出链路收口
4. 录屏状态机收敛
5. 图片历史流畅度补强

原因：

- 前两项最影响主交互稳定性
- 截图和录屏链路复杂度高，适合在统一状态机思路明确后推进
- 图片历史性能优化更依赖真实观测，可稍后跟进

---

## 9. 验收指标

建议 `P0-01` 至少达到以下结果：

- 高频场景下选词回写成功率提升
- 主窗口、截图窗口、录屏胶囊、结果窗口的行为一致性提升
- 截图输出不再出现明显语义分叉
- 录屏异常停止后的恢复能力提升
- 图片历史页在中高数据量下可用性稳定

---

## 10. 建议产出物

执行本方案时，建议同步产出：

- 高频链路问题清单
- 状态机图
- 错误码映射表
- 验收用例清单
- 回归测试清单
