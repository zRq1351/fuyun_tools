# 长截图功能深度分析报告（记忆库版）

## 分析范围

- 已自动遵循 `.gitignore`，排除忽略目录/文件（如 `node_modules`、`dist`、日志等）。
- 本报告仅聚焦“长截图”相关实现链路：
  - 前端：`src/pages/screenshot/App.vue`、`src/pages/longshot_toolbar/App.vue`、`src/services/ipc.js`
  - 后端：`src-tauri/src/features/screenshot/longshot_enabled.rs`、`src-tauri/src/features/screenshot/longshot.rs`、`src-tauri/src/ui/commands.rs`
- 目标：沉淀可检索的深度记忆，并输出两大部分：
  - 性能优化建议
  - 潜在 Bug 报告

## 深度记忆索引（可快速检索）

- `LS-PERF-001`：拼接链路 `vconcat` 逐帧全量复制，复杂度随高度增长急剧恶化。
- `LS-PERF-002`：预览图生成频率高且每次 PNG+Base64 编码，CPU 与内存分配压力大。
- `LS-PERF-003`：每帧执行多阶段对齐（模板匹配 + 相位相关 + 局部搜索），高分辨率下开销显著。
- `LS-PERF-004`：截图页导出时全画布 alpha 扫描，超大画布场景存在长尾卡顿。
- `LS-BUG-001`：长截图结果缺少高度上限，极端滚动下存在 OOM 风险。
- `LS-BUG-002`：`ffmpeg` 的 `stderr` 被管道重定向但未消费，存在管道写满阻塞风险。
- `LS-BUG-003`：命令层在 async 命令中使用阻塞睡眠，影响命令调度与响应性。
- `LS-BUG-004`：取消/结束路径等待 `join` 为同步阻塞，慢机/异常 IO 下会拖慢 UI 反馈。

## 性能优化建议

### LS-PERF-001 拼接算法逐帧全量复制（高优先级）

- 位置：
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:531-537`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:871-877`
- 现状：每次拼接都把 `existing` 与 `append` 复制到新 `Mat`，随着 `stitched` 变大，单帧成本持续上升，整体近似 O(n^2)。
- 影响：长图越长，CPU 与内存带宽占用越高，尾段拼接延迟明显。
- 建议：
  - 改为“分段缓存 + 最终一次性合并”（保存 `Vec<Mat>` 条带，结束时统一拼接/编码）。
  - 或改为流式写盘（临时无损格式）避免超大内存驻留。

### LS-PERF-002 预览图生成链路过重（高优先级）

- 位置：
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:561-589`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:904-930`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:880-902`
- 现状：定时预览每次都 `resize -> PNG 编码 -> Base64`，并发事件推送到前端。
- 影响：在 `preview_interval_ms` 较小（120~300ms）时，编码线程占用明显，GC/分配频繁。
- 建议：
  - 增加预览节流与“忙时丢帧”（上一次预览未完成则跳过本次）。
  - 仅在 `stitched_height` 有有效增量时推送预览。
  - 预览编码可降级为更轻量格式（如 JPEG 低质量）用于 UI，仅最终结果保留 PNG。

### LS-PERF-003 每帧对齐计算量偏大（中高优先级）

- 位置：
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:741-831`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:833-848`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:850-869`
- 现状：每帧都执行模板匹配、相位相关、候选 seam 评估与 ±40 行精修搜索。
- 影响：区域分辨率越高，单帧计算越重，易与预览编码叠加造成抖动。
- 建议：
  - 做自适应对齐频率：低运动段隔帧精算，普通帧复用上次位移预测。
  - 在降采样图上估计位移，再在原图做小窗口精修，减少全分辨率运算。
  - 将 `refine_overlap_rows` 的搜索窗与 `response/confidence` 动态绑定，避免固定 81 次扫描。

### LS-PERF-004 导出前覆盖层检测为全画布扫描（中优先级）

- 位置：`src/pages/screenshot/App.vue:1047-1065`
- 现状：`hasOverlayForLongshotExport()` 对整个 canvas 逐像素扫描 alpha。
- 影响：4K/长图编辑场景下，导出动作可能出现额外卡顿。
- 建议：
  - 引入 `overlayDirty` 状态位（绘制/文本/图形变更时置 true，撤销到空态时置 false），导出时 O(1) 判断。

## 潜在 Bug 报告

### LS-BUG-001 结果图高度无硬上限，存在内存耗尽风险（高风险）

- 位置：
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:437`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:531-537`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:648-652`
- 问题：`stitched` 持续增长，但未设置最大高度/最大像素量保护；极端滚动或高分辨率可导致内存暴涨。
- 风险：进程内存快速增长，触发 OOM 或系统级卡顿。
- 修复建议：
  - 增加硬阈值（如 `max_stitched_pixels`/`max_stitched_height`），超限后自动结束并提示。
  - 增加配置项并设置保守默认值。

### LS-BUG-002 `stderr` 管道未消费可能阻塞 `ffmpeg`（中高风险）

- 位置：
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:421`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:469-471`
- 问题：子进程设置了 `.stderr(Stdio::piped())`，但主循环仅读取 `stdout`，异常日志增多时可能写满 `stderr` 管道导致子进程阻塞。
- 风险：采样线程“看似卡死”，`read_exact` 长时间等待，表现为长截图停滞。
- 修复建议：
  - 将 `stderr` 改为 `Stdio::null()`（若无需消费）。
  - 或单独起线程持续 drain `stderr`，避免背压阻塞。

### LS-BUG-003 async 命令中阻塞睡眠（中风险）

- 位置：`src-tauri/src/ui/commands.rs:3074-3082`
- 问题：`start_manual_longshot` 为 async command，但内部直接 `std::thread::sleep(90ms)`。
- 风险：在命令高并发或主线程压力下造成不必要阻塞，放大响应抖动。
- 修复建议：
  - 使用 `tauri::async_runtime::sleep` 异步等待。
  - 更稳妥方案：改为前端/窗口事件确认“截图窗已隐藏”后再启动采样（事件握手替代固定时延）。

### LS-BUG-004 取消/结束为同步等待 join，可能拖慢交互（中风险）

- 位置：
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:245-247`
  - `src-tauri/src/features/screenshot/longshot_enabled.rs:283-285`
- 问题：取消和结束路径直接 `join` 工作线程，若线程正处于重计算或 IO 等待，命令返回会被阻塞。
- 风险：前端按钮点击后出现“卡住几百毫秒到数秒”的体感。
- 修复建议：
  - 采用“两阶段收口”：先原子标记并立即返回，再异步完成 `join` 与资源回收，完成后回推 lifecycle 事件。
  - 或设置可中断读取策略，缩短退出等待时间。

## 分阶段治理进展

- 第一阶段（已完成）：
  - `LS-BUG-001`：已在后端加入长截图结果安全上限（高度与总像素）并在超限时返回明确错误。
  - `LS-BUG-002`：`ffmpeg` 采样进程已将 `stderr` 改为 `Stdio::null()`，避免未消费管道背压阻塞。
  - `LS-BUG-003`：`start_manual_longshot` 中阻塞 `sleep` 已改为异步 `tauri::async_runtime::sleep`。
- 第二阶段（待推进）：
  - `LS-BUG-004`：已完成：取消路径改为“两阶段收口”（先返回、后台 `join`）；命令层 finish/cancel 改为 `spawn_blocking`，避免阻塞 async 命令线程。
  - `LS-PERF-002`：已完成第一步：预览仅在拼接高度增长时才触发编码/推送，降低重复 PNG+Base64 开销。
  - `LS-PERF-004`：已完成：导出前覆盖层判断改为 `overlayDirty` 脏标记，移除导出时全画布 alpha 扫描。
  - `LS-PERF-001`：已完成第一步：拼接改为“分段缓存（Vec<Mat>）+ 收尾一次性合并”，移除逐帧 `vconcat` 全量复制。
  - `LS-PERF-003`：已完成：对齐链路改为“降采样粗估 + 原图小窗精修 + 动态搜索窗”，降低每帧对齐计算成本。

## 结论

- 当前长截图实现在“功能完整性”上较成熟，但性能压力主要集中在：`逐帧全量拼接` 与 `高频预览编码`。
- 稳定性风险主要集中在：`内存无上限保护`、`子进程 stderr 管道阻塞`、`阻塞式收口`。
- 建议优先级：
  - P0：`LS-PERF-001` + `LS-BUG-001`
  - P1：`LS-PERF-002` + `LS-BUG-002`
  - P2：`LS-PERF-003` + `LS-BUG-003` + `LS-BUG-004`
