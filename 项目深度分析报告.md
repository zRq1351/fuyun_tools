# fuyun_tools 项目深度分析报告

> 分析方式：基于 `git ls-files` 的有效代码全集进行静态深读，自动排除 `.gitignore` 覆盖的忽略目录与构建产物（如
`node_modules`、`dist`、`target`、`src-tauri/bin`、日志目录等）。

## 一、分析范围与上下文

- 后端：`src-tauri/src`（核心状态、命令层、录屏、截图、剪贴板、数据库与图片存储）
- 前端：`src/pages` 多窗口页面 + `src/services/ipc.js` IPC 抽象
- 构建与配置：`src-tauri/Cargo.toml`、`src/vite.config.js`、`src-tauri/tauri.conf.json`
- 目标：构建可复用“项目记忆库”，用于性能优化与潜在 Bug 快速定位

## 二、项目深度记忆库（Memory Index）

### 2.1 架构记忆

- `ARCH-01` 多窗口架构
    - 前端采用 Vite 多入口页面（设置、截图、录屏胶囊、图片预览等），Tauri 管理多 WebviewWindow。
    - 参考：`src/vite.config.js`，`src-tauri/tauri.conf.json`

- `ARCH-02` 状态中枢
    - `AppState` 统一管理文本/图片剪贴板、录屏运行时、窗口可见性与操作序列。
    - 参考：`src-tauri/src/core/app_state.rs`

- `ARCH-03` IPC 边界
    - 前端通过 `src/services/ipc.js` 聚合命令常量与服务方法；Rust `ui/commands*.rs` 承接命令并路由业务。
    - 参考：`src/services/ipc.js`，`src-tauri/src/ui/commands.rs`，`src-tauri/src/ui/commands_recording.rs`

### 2.2 热点记忆（高收益排查入口）

- `HOT-REC-01` 录屏停止与清理链路（最敏感）
    - `stop_recording/cancel_recording` 涉及进程控制、线程 join、文件重命名、音频合成与状态收口。
    - 参考：`src-tauri/src/features/recording/recorder_service.rs`

- `HOT-SS-01` 截图启动桥接
    - `screenshot/main.js` 写 boot 数据，`screenshot/App.vue` 消费 boot 数据并驱动模式与 fallback。
    - 参考：`src/pages/screenshot/main.js`，`src/pages/screenshot/App.vue`

- `HOT-DB-01` 文本历史快照持久化
    - `save_history_data_snapshot_async` 全量重建 history/categories/pinned/fts。
    - 参考：`src-tauri/src/utils/database.rs`

- `HOT-FE-01` 设置页开发指标轮询
    - `setInterval(async...)` 可能并发重入，导致额外 IPC 压力。
    - 参考：`src/pages/settings/components/DeveloperSettings.vue`

## 三、性能优化建议

### P1. 录屏停止路径持锁过长（高优先级）

- **位置**
    - `src-tauri/src/features/recording/recorder_service.rs:1129-1309`
- **现象**
    - 在持有 `runtime` 锁期间执行：`try_wait + sleep`、`join`、`fs::rename/remove_file`、`merge_system_audio_into_video`
      等慢操作。
- **风险**
    - 放大锁竞争，导致状态查询/命令调用卡顿，前端感知“停止很慢”。
- **建议**
    - 改成三段式：
    - 第 1 段（锁内）：只做状态切换与必要句柄快照；
    - 第 2 段（锁外）：执行等待进程、线程回收、IO 与合成；
    - 第 3 段（锁内）：统一回写结果并 `reset_to_idle()`。

### P2. 取消录制同类问题（高优先级）

- **位置**
    - `src-tauri/src/features/recording/recorder_service.rs:1320-1361`
- **现象**
    - `cancel_recording` 持锁执行 `kill/wait/join/remove_file`。
- **风险**
    - 与 `stop_recording` 相同，容易阻塞并发状态操作。
- **建议**
    - 与 P1 同步重构为“锁内快照 + 锁外慢清理 + 锁内收口”。

### P3. 文本历史快照全量重建写放大

- **位置**
    - `src-tauri/src/utils/database.rs:669-757`
- **现象**
    - 全量 `DELETE` + 全量 `INSERT` + FTS 重建。
- **风险**
    - 数据量上升后 I/O 与事务耗时增长，可能影响主流程响应。
- **建议**
    - 改增量 Upsert（按变更集写入）；
    - FTS 改增量维护；
    - 大事务分批提交（必要时）。

### P4. Async 命令承载同步重活

- **位置**
    - `src-tauri/src/ui/commands_recording.rs:243-334`
- **现象**
    - `#[tauri::command] async fn` 直接调用同步重量函数。
- **风险**
    - 可能占用 runtime worker，降低并发命令吞吐。
- **建议**
    - 使用 `tauri::async_runtime::spawn_blocking` 包装同步重活，或服务层逐步 async 化。

## 四、潜在 Bug 报告

### B1. 截图启动模式可能丢失（高风险）

- **位置**
    - `src/pages/screenshot/main.js:17-20`
    - `src/pages/screenshot/App.vue:547-551`
- **问题**
    - 启动桥只记录 `pendingStartSessionId`，未同步 `pendingMode`；
    - `consumeBootPayload` 读取 `pendingMode`，缺失时回退默认 `screenshot`。
- **影响**
    - 冷启动或事件时序波动下，模式错乱（如 OCR/取色预期不一致）。
- **修复建议**
    - 在 `start-region-select` 监听中写入：
    - `screenshotBoot.pendingMode = event?.detail?.mode || 'screenshot'`。

### B2. 快捷键录制监听可能残留（中高）

- **位置**
    - `src/pages/settings/composables/useShortcutRecorder.js:15-18, 69-74`
- **问题**
    - composable 内没有 `onUnmounted` 兜底；若录制中组件卸载，`document keydown` 监听可能残留。
- **影响**
    - 键盘事件被意外拦截，页面行为异常。
- **修复建议**
    - 在 composable 内增加：`onUnmounted(() => stopRecording())`。

### B3. 录屏停止阶段可感知卡顿风险（中高）

- **位置**
    - `src-tauri/src/features/recording/recorder_service.rs:1167-1178`
- **问题**
    - 最长等待进程退出约 8 秒（80 * 100ms），且在持锁路径中执行。
- **影响**
    - 并发命令或状态轮询出现明显延迟。
- **修复建议**
    - 同 P1：缩短锁占用 + 分段超时 + 错误态快速收口。

### B4. 预览生成器全局锁 `unwrap` 潜在崩溃点（中）

- **位置**
    - `src-tauri/src/utils/image_clipboard.rs:139-170, 195-245, 866-867`
- **问题**
    - `std::sync::Mutex` 在中毒场景 `lock().unwrap()` 可能直接 panic。
- **影响**
    - 后台线程异常后可能触发连锁崩溃。
- **修复建议**
    - 改 `unwrap_or_else(|p| p.into_inner())`，或统一迁移到 `parking_lot::Mutex`。

### B6. 截图 fallback 与请求时序竞态（中）

- **位置**
    - `src/pages/screenshot/App.vue:555-565, 596-605`
- **问题**
    - 120ms fallback 可能与正常事件竞态，导致额外 `start_screenshot` 请求。
- **影响**
    - 轻度重复工作、日志噪音与时序复杂化。
- **修复建议**
    - 增加会话 in-flight 标记 / session 去重，确保同会话只触发一次截图请求。

### B7. 录屏快捷键高频触发可能堆积任务（中）

- **位置**
    - `src-tauri/src/lib.rs:179-186`
- **问题**
    - 按键回调每次 `spawn` 异步任务，缺少 in-flight 门闩。
- **影响**
    - 高频触发下可能出现任务堆积与状态抖动。
- **修复建议**
    - 增加原子标志或最小触发间隔（防抖）

