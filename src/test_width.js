const css = `
.clipboard-item {
  width: 250px;
  margin-right: 8px;
  border: 1px solid rgba(255, 255, 255, 0.1);
  padding: 12px;
  box-sizing: border-box;
}
`
// with box-sizing border-box, width is exactly 250.
// 250 + 8 (margin) = 258.
